<module>

	<!-- Inherit DefaultWidgetSet -->
	<inherits name="com.vaadin.terminal.gwt.DefaultWidgetSet" />
    
    <inherits name="com.google.gwt.json.JSON" />
    
    <!-- Inherit CHAP Links Timeline and GWT Visualization. -->
    <inherits name="com.google.gwt.visualization.Visualization" />
    <inherits name="com.chap.links.Timeline" />
	
    <!-- Inherit the default GWT style sheet.  You can change       -->
    <!-- the theme of your GWT application by uncommenting          -->
    <!-- any one of the following lines.                            -->
    <!-- <inherits name='com.google.gwt.user.theme.standard.Standard'/>  -->
    <!-- <inherits name='com.google.gwt.user.theme.chrome.Chrome'/> -->
    <!-- <inherits name='com.google.gwt.user.theme.dark.Dark'/>     -->
    
    <!-- Explicitly include client and shared packages. -->
    <source path="client" />
    <source path="shared" />
    
	<!-- Widgetset default theme -->
	<!-- This is relative to widgetset_directory/public -->	
	<stylesheet src="timeline/styles.css"/>
    
    <!--
     Uncomment the following to compile the widgetset for one browser only.
     This can reduce the GWT compilation time significantly when debugging.
     The line should be commented out before deployment to production
     environments.
      
     Multiple browsers can be specified for GWT 2.0.3 as a comma separated
     list. The supported user agents at the moment of writing were:
     ie6,ie8,gecko,gecko1_8,safari,opera
     
     The value gecko1_8 is used for Firefox 3 and later and safari is used
     for webkit based browsers including Google Chrome.
    -->
    <set-property name="user.agent" value="gecko1_8"/>
   
</module>
